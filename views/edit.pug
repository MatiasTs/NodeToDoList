extends layout
block content
    .container
        h1.titulo= title 
        form.formulario(action=`/edit/${task.id}` method="POST")
            label(for="title") Título:
            input(type="text", name="title", id="title", value=task.title)
            button.button Actualizar